Pour résoudre votre erreur 404 spécifique :

Vérifiez vos informations d'identification AWS :
frapperaws configure list
aws sts get-caller-identity

Vérifiez que le modèle existe dans l'environnement source avant la promotion
Installer la librairie manquante :
frapperpip install mlflow-export-import

Vérifiez vos paramètres Databricks dans la fonction main()avec vos vraies valeurs

L'erreur 404 vient probablement du fait que databricks_api_helper.get_model_registration()ne trouve pas le modèle spécifié dans votre environnement source.
